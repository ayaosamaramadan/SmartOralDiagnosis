import SectionPlaceholder from "../../components/SectionPlaceholder";

export default function DoctorsPage() {
  return (
    <SectionPlaceholder
      title="Doctors"
      description="This directory page is reserved for the doctor management UI. It is available as a route now, instead of returning 404."
    />
  );
}