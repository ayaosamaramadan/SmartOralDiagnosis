import SectionPlaceholder from "../../components/SectionPlaceholder";

export default function PatientsPage() {
  return (
    <SectionPlaceholder
      title="Patients"
      description="This section is ready as a route, but the detailed patient management screen still needs to be built."
    />
  );
}