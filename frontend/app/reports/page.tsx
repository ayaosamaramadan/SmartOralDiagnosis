import SectionPlaceholder from "../../components/SectionPlaceholder";

export default function ReportsPage() {
  return (
    <SectionPlaceholder
      title="Reports"
      description="This reporting screen is not implemented yet, but the route now exists so admin navigation stays valid."
    />
  );
}