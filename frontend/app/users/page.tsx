import SectionPlaceholder from "../../components/SectionPlaceholder";

export default function UsersPage() {
  return (
    <SectionPlaceholder
      title="Users"
      description="This admin section is not wired to a dedicated frontend table yet. The route is available so navigation no longer breaks."
    />
  );
}